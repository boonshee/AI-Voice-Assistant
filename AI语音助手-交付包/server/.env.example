# 复制本文件为 .env 后填写，勿提交 .env 到 Git
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_MS=30000
HOST=0.0.0.0
PORT=3001
CORS_ORIGIN=http://localhost:5173,https://localhost,capacitor://localhost
# 可选：与 APK 构建时的 VITE_PROXY_TOKEN 一致，启用后 /api/chat 需带 X-Proxy-Token
# PROXY_AUTH_TOKEN=
