# 构建时默认云端 Proxy（装完即用，无需客户填 IP）
# 复制为 .env 后填入真实 Worker 地址，例如：
# VITE_API_BASE_URL=https://voice-ai-proxy.xxx.workers.dev

# 可选：与 Worker/Server 的 PROXY_AUTH_TOKEN 一致（双端须同时配置）
# VITE_PROXY_TOKEN=your-token

# 本地开发 / 局域网调试时可改为：
# VITE_API_BASE_URL=http://localhost:3001
# Android 模拟器：http://10.0.2.2:3001
